import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const RecipeAddScreen = () => {
  return (
    <View>
      <Text>RecipeAddScreen</Text>
    </View>
  );
};

export default RecipeAddScreen;

const styles = StyleSheet.create({});
